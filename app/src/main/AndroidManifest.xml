<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.food.hungerbee">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logocircular"
        android:supportsRtl="true"
        android:theme="@style/Theme.HungerBee">
        <activity android:name=".HeadSettingActivity"></activity>
        <activity android:name=".AdminOrderHistoryActivity" />
        <activity android:name=".HeadDashboardActivity" />
        <activity android:name=".HeadRestaurantDetails" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".UserCartActivity" />
        <activity android:name=".HeadAccessChangingActivity" />
        <activity android:name=".UserRestaurantActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".UserFullCategoryList" />
        <activity android:name=".AdminFoodUploadActivity" />
        <activity android:name=".AdminOurFoodsActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstTimeProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UserProfile" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>